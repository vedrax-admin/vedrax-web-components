/**
 * Enum that describes the available control types
 */
export enum ControlType {
    checkbox = "checkbox",
    datepicker = "datepicker",
    input = "input",
    radioButton = "radioButton",
    select = "select",
    //slider = "slider",
    arrayOfControls = "arrayOfControls",
    search = "search",
    matrix = "matrix",
    nvp = "nvp"
}